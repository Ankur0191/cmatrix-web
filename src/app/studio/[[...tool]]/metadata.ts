// src/app/studio/[[...tool]]/metadata.ts
import { metadata as studioMetadata, viewport as studioViewport } from 'next-sanity/studio'

export const metadata = studioMetadata
export const viewport = studioViewport
